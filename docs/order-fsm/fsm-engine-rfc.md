# FSM Engine RFC — развитие ядра FSM (guard / effect / context / timer / registry)

> **Кому:** Максим — автор ядра FSM (ХП + табличный движок, референсная постаматная реализация).
> **От:** Павел (аналитика/спецификации), по итогам сверки FSM-ядра с Иваном **2026-06-26**.
> **Что это:** запрос на комментарий (RFC) **только по развитию самого движка**. Под Vote-заказ такси
> (и потенциально других workflow) нам нужны декларативные **guard / effect** на переходах, **таймеры
> состояния** (как внешний источник действий), ясность по **контексту процесса** и понимание места
> **registry**. Хотим заложить это как механизмы ядра, **не сломав универсальность** движка (он вырос из
> доставки через постаматы — ты его носитель). Нужен твой ответ именно как автора ядра.
>
> **Вне scope — читать не нужно:** контракт бота, Query API, `availableActions`, ObservedState/UI,
> бизнес-правила такси. Это слои Павла/Ивана; твой ответ от них не зависит.
>
> Вопросов 9, но каждый короткий — можно отвечать тезисно, даже «да / нет / так нельзя, потому что…».

> **🟢 Статус (обновл. 2026-06-30): часть вопросов уже закрыта схемно Иваном.** В обновлённом seed
> (`fsm_spec.py` + `taxi_order_fsm_seed.sql`, v2) `fsm_transitions` расширена forward-compat полями
> `guard_name` / `guard_params` / `effect_name` / `effect_params` / `timer_name` / `timer_params`
> (`VARCHAR(100)` для имён, `JSON` для params; ALTER идемпотентны). Значения **везде NULL** — граф
> (12 состояний / 13 действий / 21 переход) не изменён, поведение прежнее. Это фактический ответ
> «да, расширяем» на **Q1** (расширяемость) и **Q7** (поля сейчас, NULL) и заявка на размещение таймера
> как атрибута перехода (**Q4/Q5**). **Остаются открытыми именно механизмы исполнения:** где исполняется
> guard (**Q2**), атомарность (**Q3**), модель Context (**Q4**), registry (**Q6**), семантика таймера
> (**Q5**: какое timeout-действие выстреливает и как таймер снимается при выходе из состояния).

---

## Уровни, которые мы НЕ хотим смешивать

guard, effect и timer — на **разных уровнях**, и объединять их в один механизм было бы ошибкой. Как мы это
видим (поправь, если в ядре иначе):

**Guard и effect — фазы одного перехода:**
```
Действие (запрос перехода)
        ↓
     Guard            — можно ли перейти?
        ↓
  Смена состояния
        ↓
     Effect           — что сделать после
        ↓
     Журнал
```

**Таймер — внешний источник действий, а НЕ разновидность effect:**
```
Timer  →  Action  →  (тот же pipeline перехода выше)
```
Таймер сам ничего не меняет — он лишь порождает действие (`timeout`), которое дальше идёт через обычный
переход (guard → смена состояния → effect). Вопросы ниже сгруппированы по этим уровням.

---

## Контекст: как мы понимаем движок сейчас (поправь, если не так)

Источник — дамп `vote_fsm` и описание Ивана. Модель ниже — то, как мы её поняли; твоя правка авторитетна.

- Движок **табличный**: переходы в `fsm_states` / `fsm_actions` / `fsm_transitions`; исполнение — ХП
  `fsm_perform_action`; журнал переходов — `fsm_action_logs`.
- Состояние сущности хранится в её таблице (`orders.status`).
- **Отдельного guard-слоя в ХП нет** — условия сейчас проверяются в приложении до вызова `fsm_perform_action`.
- **Универсального timer subsystem нет** — таймеры состояния (expire, no-show) в рантайме не срабатывают.
- Side-effects перехода держатся в action layer / worker, **не внутри ХП**.

Цель — добавить это как **универсальные механизмы движка**, а не taxi-only заплатки. Там, где это
противоречит архитектуре ядра, твоё «нет, так нельзя» для нас ценнее нашей гипотезы.

---

## Вопросы

### Переход: guard / effect / атомарность

**1. Расширяемость `fsm_transitions`.** Поддерживает ли архитектура ХП добавление в таблицу переходов
полей `guard_name`, `guard_params`, `effect_name`, `effect_params` — без слома существующего
(постаматного) движка? Это естественное расширение текущей модели или чужеродный для неё элемент?

**2. Где должен жить guard?** Какой вариант правильнее с точки зрения архитектуры ядра:
- (a) **в приложении**, до `fsm_perform_action` (как сейчас де-факто);
- (b) **внутри** `fsm_perform_action`;
- (c) **внутри транзакции перехода** (после выбора перехода, но до фиксации состояния).

**3. Атомарность перехода.** Что ты считаешь минимальной атомарной единицей выполнения перехода? Должны
ли guard, смена состояния, side effects и журнал находиться в одной транзакции или допустимо их
разделение? Какие гарантии мы хотим от ХП и чем можно/нужно пожертвовать (например, консистентностью
журнала)?

### Контекст процесса

**4. Context в модели ядра.** guard и effect предполагают доступ к данным процесса. Здесь важно различать
две разные вещи:
- **логический контекст** — сущности процесса (`Order`, `Candidate`, `Offer`, `Driver`, активные таймеры);
- **физический способ его передачи** — JSON, DTO, `SQL JOIN` и т.п.

Нас интересует именно логический уровень. Главный вопрос архитектурный: **является ли `Context` частью
модели ядра** — или ядро вообще не знает о существовании контекста и получает на вход уже всё, что нужно
переходу? Если `Context` является частью модели ядра:
- (a) контекст **полностью передаётся приложением** (ядро его не собирает, получает на вход);
- (b) контекст **собирается движком** (ядро умеет его читать).

Способ передачи (JSON/DTO/JOIN) — твоё дело как автора ядра, мы его не навязываем.

### Таймеры

**5. Таймер — особый механизм или просто источник Action?** Следует ли рассматривать таймер как обычный
источник команды (`timeout`), которая проходит через стандартный pipeline перехода (guard → смена
состояния → effect), или для него нужна отдельная подсистема в ядре? *Вторично, для реализации worker'а по
`next_timer_at`:* он (a) меняет состояние напрямую; (b) вызывает `fsm_perform_action(timeout)`; (c) идёт
тем же входом, что и пользовательские действия.

### Схема и расширяемость

**6. Чья ответственность — registry.** Шире, чем «нужен ли он сейчас»: **должно ли ядро вообще знать о
`GuardRegistry` / `EffectRegistry`** (разрешение guard и effect по имени) — или разрешение имён всегда
остаётся ответственностью приложения? Возможны оба варианта, и нам важно, какой из них ты считаешь
правильным для движка. Если registry — всё-таки зона ядра: закладывать его сразу или ввести позже, а пока
оставить разрешение имён в приложении?

**7. Поля сейчас или позже?** Имеет ли смысл добавить `guard_name` / `effect_name` в `fsm_transitions`
**уже сейчас** (пустыми / NULL), чтобы не делать миграцию схемы позже, когда механизм заработает?

**8. Ограничения текущей архитектуры.** Есть ли в текущей ХП-архитектуре ограничения, которые могут
помешать такому развитию (guard / effect / timer / context / registry)? Какие риски ты как автор ядра
видишь для устойчивости и универсальности движка?

### Взгляд автора

**9. Если бы заново.** Если бы ты проектировал движок сегодня — изменил бы что-то **принципиально** в его
архитектуре, с учётом опыта постаматов и новых требований? (Если ответ «ничего принципиально не менял бы» —
это для нас тоже очень ценный результат.)

---

## Что дальше

По итогам твоих ответов скорректируем [fsm-core-design.md](fsm-core-design.md) и определим дальнейшее
направление развития ядра FSM. Ответ вида «это taxi-only, в ядро не нужно» — для нас такой же ценный
результат, как «да, расширяем движок».
